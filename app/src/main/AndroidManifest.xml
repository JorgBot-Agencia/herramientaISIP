<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.formato.isp">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <protected-broadcast android:name="android.intent.action.MEDIA_MOUNTED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".DesarrolloEncuesta.menuEncuesta"></activity>
        <activity android:name=".DesarrolloEncuesta.reporteGrafico" />
        <activity android:name=".DesarrolloEncuesta.cargando" />
        <activity android:name=".GestionEmpresa.buscar_empresa" />
        <activity android:name=".GestionEmpresa.infoDetallada" />
        <activity android:name=".GestionEmpresa.registroEmpresa" />
        <activity android:name=".DesarrolloEncuesta.opcionPrincipal" />
        <activity android:name=".DesarrolloEncuesta.preguntasEncuesta" />
        <activity android:name=".GestionFundacion.registroFundacion" />
        <activity
            android:name=".MenuLateral.menuprincipal"
            android:label="@string/title_activity_me"
            android:theme="@style/NoActionBar" />
        <activity android:name=".informacionEmpresarial" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>